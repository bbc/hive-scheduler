<head>
  <% provide(:title, "Create A New Script") %>
</head>

<% title = t('.title', default: [:'helpers.titles.new', 'Create a new %{model}'], model: Script.model_name.human.titleize) %>
<%= render partial: "form", locals: { title: title } %>
